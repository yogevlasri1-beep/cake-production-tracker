-- HACCP workspace: plant team + plans per product family (category group).
SELECT public.create_kitchen_sync_table('sync_haccp_team_members');
SELECT public.create_kitchen_sync_table('sync_haccp_plans');
